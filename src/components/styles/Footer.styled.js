import styled from 'styled-components'

export const StyledFooter = styled.div`
    flex-shrink: 0;
    text-align: center;
    color: white;
    font-weight: 300;

    a {
        text-decoration: none;
        color: inherit;
    }
`